spring:
  application:
    name: om-bank-assessment
  datasource:
    url:  jdbc:h2:mem:bankdb
    driver-class-name:  org.h2.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
